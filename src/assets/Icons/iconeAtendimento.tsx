import { Bell } from 'lucide-react'
import type { TypeClassIcon } from '../../types/interface-class-icon'

export function IconeAtendimento({ className }: TypeClassIcon) {
  return (
    <Bell className={className}/>
  )
}
